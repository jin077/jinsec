<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.team.mapper.WeatherMapper">
  <insert id="weatherInsert" parameterType="com.team.dto.item">
    insert into shortWeather(baseDate, baseTime, category, fcstDate, fcstValue, nx, ny, updated)
    values(#{baseDate},#{baseTime},#{category},#{fcstDate},#{fcstValue},#{nx},#{ny}, 0)
  </insert>
</mapper>